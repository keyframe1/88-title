# Normalize line endings: store text as LF in the repo (consistent on Vercel/Linux).
* text=auto eol=lf

# Treat known binaries as binary (no EOL conversion).
*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.woff binary
*.woff2 binary
